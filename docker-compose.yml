services:
  notification-service:
    build: .
    container_name: notification-service
    ports:
      - "${PORT:-6752}:${PORT:-6752}"
    env_file:
      - .env.prod
    environment:
      - DEV_ENV=production
    networks:
      - dass_private

networks:
  dass_private:
    external: true
